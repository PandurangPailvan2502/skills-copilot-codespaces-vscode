// Create web server
// Create a web server that listens on port 3000 and serves the comments.html file. Use the fs module to read the file and send it to the client.
// The comments.html file should be in the same directory as the comments.js file.
// The server should respond to GET requests to the /comments URL.
// The server should respond with a 200 status code.
// The server should respond with the contents of the comments.html file.
// The server should respond with the correct Content-Type header.
// The server should respond with the correct Content-Length header.
// The server should respond with the correct Content-Encoding header.
// The server should respond with the correct Transfer-Encoding header.
// The server should respond with the correct Date header.
